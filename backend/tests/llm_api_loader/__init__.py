"""Lightweight loader package for LLM provider clients (test copy).

This is a test-local copy of the production module so tests can run from
`backend/` without relying on the repository root in PYTHONPATH.
"""

