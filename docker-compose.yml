services:
  backend:
    build:
      context: ./backend
    environment:
      # Explicit path so it works inside container workdir (/app)
      DATABASE_URL: ${DATABASE_URL:-sqlite:///./data.db}
      SECRET_KEY: ${SECRET_KEY:-dev-secret-key-change-me}
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:5173}
      # LLM provider (Qwen/DashScope)
      LLM_PROVIDER: ${LLM_PROVIDER:-qwen}
      DASHSCOPE_API_KEY: ${DASHSCOPE_API_KEY:-}
      DASHSCOPE_BASE_URL: ${DASHSCOPE_BASE_URL:-https://dashscope.aliyuncs.com/api/v1}
      MODEL_NAME: ${MODEL_NAME:-qwen3-vl-plus}
      ENABLE_THINKING: ${ENABLE_THINKING:-true}
      THINKING_BUDGET: ${THINKING_BUDGET:-50}
      # LLM judge (optional): pass-through from .env if set
      USE_LLM_JUDGE_EN2ZH: ${USE_LLM_JUDGE_EN2ZH:-}
      LLM_JUDGE_STRICTNESS: ${LLM_JUDGE_STRICTNESS:-}
      LLM_JUDGE_TREAT_PARTIAL_AS_CORRECT: ${LLM_JUDGE_TREAT_PARTIAL_AS_CORRECT:-}
      # Logging / tracing (optional)
      AGENT_LOG_LEVEL: ${AGENT_LOG_LEVEL:-}
      LLM_DEBUG: ${LLM_DEBUG:-}
      LANGSMITH_ENABLED: ${LANGSMITH_ENABLED:-}
      LANGCHAIN_TRACING_V2: ${LANGCHAIN_TRACING_V2:-}
      LANGCHAIN_API_KEY: ${LANGCHAIN_API_KEY:-}
      LANGCHAIN_PROJECT: ${LANGCHAIN_PROJECT:-}
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:8000/health').status==200 else 1)"]
      interval: 10s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      # Default Dockerfile builds static site + nginx (prod). Dev override switches Dockerfile.
      args:
        VITE_API_BASE: ${VITE_API_BASE:-/api}
    depends_on:
      - backend
    restart: unless-stopped

networks:
  default:
    name: sidebyside_net
